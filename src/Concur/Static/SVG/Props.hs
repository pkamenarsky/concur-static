-- | SVG properties/attributes
--
-- See: <https://developer.mozilla.org/en-US/docs/Web/SVG/Attribute>
module Concur.Static.SVG.Props where

-- Note that this module is auto-generated.
-- See @./misc/gen-svg-modules@ for details.

import Concur.Static (Props)
import Concur.Static.DOM.Props (textProp)

accentHeight :: String -> Props a
accentHeight = textProp "accent-height"

accumulate :: String -> Props a
accumulate = textProp "accumulate"

additive :: String -> Props a
additive = textProp "additive"

alignmentBaseline :: String -> Props a
alignmentBaseline = textProp "alignment-baseline"

allowReorder :: String -> Props a
allowReorder = textProp "allowReorder"

alphabetic :: String -> Props a
alphabetic = textProp "alphabetic"

amplitude :: String -> Props a
amplitude = textProp "amplitude"

arabicForm :: String -> Props a
arabicForm = textProp "arabic-form"

ascent :: String -> Props a
ascent = textProp "ascent"

attributeName :: String -> Props a
attributeName = textProp "attributeName"

attributeType :: String -> Props a
attributeType = textProp "attributeType"

autoReverse :: String -> Props a
autoReverse = textProp "autoReverse"

azimuth :: String -> Props a
azimuth = textProp "azimuth"

baseFrequency :: String -> Props a
baseFrequency = textProp "baseFrequency"

baselineShift :: String -> Props a
baselineShift = textProp "baseline-shift"

baseProfile :: String -> Props a
baseProfile = textProp "baseProfile"

bbox :: String -> Props a
bbox = textProp "bbox"

begin :: String -> Props a
begin = textProp "begin"

bias :: String -> Props a
bias = textProp "bias"

by :: String -> Props a
by = textProp "by"

calcMode :: String -> Props a
calcMode = textProp "calcMode"

capHeight :: String -> Props a
capHeight = textProp "cap-height"

className :: String -> Props a
className = textProp "class"

clip :: String -> Props a
clip = textProp "clip"

clipPathUnits :: String -> Props a
clipPathUnits = textProp "clipPathUnits"

clipPath :: String -> Props a
clipPath = textProp "clip-path"

clipRule :: String -> Props a
clipRule = textProp "clip-rule"

color :: String -> Props a
color = textProp "color"

colorInterpolation :: String -> Props a
colorInterpolation = textProp "color-interpolation"

colorInterpolationFilters :: String -> Props a
colorInterpolationFilters = textProp "color-interpolation-filters"

colorProfile :: String -> Props a
colorProfile = textProp "color-profile"

colorRendering :: String -> Props a
colorRendering = textProp "color-rendering"

contentScriptType :: String -> Props a
contentScriptType = textProp "contentScriptType"

contentStyleType :: String -> Props a
contentStyleType = textProp "contentStyleType"

cursor :: String -> Props a
cursor = textProp "cursor"

cx :: String -> Props a
cx = textProp "cx"

cy :: String -> Props a
cy = textProp "cy"

d :: String -> Props a
d = textProp "d"

decelerate :: String -> Props a
decelerate = textProp "decelerate"

descent :: String -> Props a
descent = textProp "descent"

diffuseConstant :: String -> Props a
diffuseConstant = textProp "diffuseConstant"

direction :: String -> Props a
direction = textProp "direction"

display :: String -> Props a
display = textProp "display"

divisor :: String -> Props a
divisor = textProp "divisor"

dominantBaseline :: String -> Props a
dominantBaseline = textProp "dominant-baseline"

dur :: String -> Props a
dur = textProp "dur"

dx :: String -> Props a
dx = textProp "dx"

dy :: String -> Props a
dy = textProp "dy"

edgeMode :: String -> Props a
edgeMode = textProp "edgeMode"

elevation :: String -> Props a
elevation = textProp "elevation"

enableBackground :: String -> Props a
enableBackground = textProp "enable-background"

end :: String -> Props a
end = textProp "end"

exponent :: String -> Props a
exponent = textProp "exponent"

externalResourcesRequired :: String -> Props a
externalResourcesRequired = textProp "externalResourcesRequired"

fill :: String -> Props a
fill = textProp "fill"

fillOpacity :: String -> Props a
fillOpacity = textProp "fill-opacity"

fillRule :: String -> Props a
fillRule = textProp "fill-rule"

filter :: String -> Props a
filter = textProp "filter"

filterRes :: String -> Props a
filterRes = textProp "filterRes"

filterUnits :: String -> Props a
filterUnits = textProp "filterUnits"

floodColor :: String -> Props a
floodColor = textProp "flood-color"

floodOpacity :: String -> Props a
floodOpacity = textProp "flood-opacity"

fontFamily :: String -> Props a
fontFamily = textProp "font-family"

fontSize :: String -> Props a
fontSize = textProp "font-size"

fontSizeAdjust :: String -> Props a
fontSizeAdjust = textProp "font-size-adjust"

fontStretch :: String -> Props a
fontStretch = textProp "font-stretch"

fontStyle :: String -> Props a
fontStyle = textProp "font-style"

fontVariant :: String -> Props a
fontVariant = textProp "font-variant"

fontWeight :: String -> Props a
fontWeight = textProp "font-weight"

format :: String -> Props a
format = textProp "format"

from :: String -> Props a
from = textProp "from"

fr :: String -> Props a
fr = textProp "fr"

fx :: String -> Props a
fx = textProp "fx"

fy :: String -> Props a
fy = textProp "fy"

g1 :: String -> Props a
g1 = textProp "g1"

g2 :: String -> Props a
g2 = textProp "g2"

glyphName :: String -> Props a
glyphName = textProp "glyph-name"

glyphOrientationHorizontal :: String -> Props a
glyphOrientationHorizontal = textProp "glyph-orientation-horizontal"

glyphOrientationVertical :: String -> Props a
glyphOrientationVertical = textProp "glyph-orientation-vertical"

glyphRef :: String -> Props a
glyphRef = textProp "glyphRef"

gradientTransform :: String -> Props a
gradientTransform = textProp "gradientTransform"

gradientUnits :: String -> Props a
gradientUnits = textProp "gradientUnits"

hanging :: String -> Props a
hanging = textProp "hanging"

height :: String -> Props a
height = textProp "height"

href :: String -> Props a
href = textProp "href"

hreflang :: String -> Props a
hreflang = textProp "hreflang"

horizAdvX :: String -> Props a
horizAdvX = textProp "horiz-adv-x"

horizOriginX :: String -> Props a
horizOriginX = textProp "horiz-origin-x"

id :: String -> Props a
id = textProp "id"

ideographic :: String -> Props a
ideographic = textProp "ideographic"

imageRendering :: String -> Props a
imageRendering = textProp "image-rendering"

in_ :: String -> Props a
in_ = textProp "in"

in2 :: String -> Props a
in2 = textProp "in2"

intercept :: String -> Props a
intercept = textProp "intercept"

k :: String -> Props a
k = textProp "k"

k1 :: String -> Props a
k1 = textProp "k1"

k2 :: String -> Props a
k2 = textProp "k2"

k3 :: String -> Props a
k3 = textProp "k3"

k4 :: String -> Props a
k4 = textProp "k4"

kernelMatrix :: String -> Props a
kernelMatrix = textProp "kernelMatrix"

kernelUnitLength :: String -> Props a
kernelUnitLength = textProp "kernelUnitLength"

kerning :: String -> Props a
kerning = textProp "kerning"

keyPoints :: String -> Props a
keyPoints = textProp "keyPoints"

keySplines :: String -> Props a
keySplines = textProp "keySplines"

keyTimes :: String -> Props a
keyTimes = textProp "keyTimes"

lang :: String -> Props a
lang = textProp "lang"

lengthAdjust :: String -> Props a
lengthAdjust = textProp "lengthAdjust"

letterSpacing :: String -> Props a
letterSpacing = textProp "letter-spacing"

lightingColor :: String -> Props a
lightingColor = textProp "lighting-color"

limitingConeAngle :: String -> Props a
limitingConeAngle = textProp "limitingConeAngle"

local :: String -> Props a
local = textProp "local"

markerEnd :: String -> Props a
markerEnd = textProp "marker-end"

markerMid :: String -> Props a
markerMid = textProp "marker-mid"

markerStart :: String -> Props a
markerStart = textProp "marker-start"

markerHeight :: String -> Props a
markerHeight = textProp "markerHeight"

markerUnits :: String -> Props a
markerUnits = textProp "markerUnits"

markerWidth :: String -> Props a
markerWidth = textProp "markerWidth"

mask :: String -> Props a
mask = textProp "mask"

maskContentUnits :: String -> Props a
maskContentUnits = textProp "maskContentUnits"

maskUnits :: String -> Props a
maskUnits = textProp "maskUnits"

mathematical :: String -> Props a
mathematical = textProp "mathematical"

max :: String -> Props a
max = textProp "max"

media :: String -> Props a
media = textProp "media"

method :: String -> Props a
method = textProp "method"

min :: String -> Props a
min = textProp "min"

mode :: String -> Props a
mode = textProp "mode"

name :: String -> Props a
name = textProp "name"

numOctaves :: String -> Props a
numOctaves = textProp "numOctaves"

offset :: String -> Props a
offset = textProp "offset"

opacity :: String -> Props a
opacity = textProp "opacity"

operator :: String -> Props a
operator = textProp "operator"

order :: String -> Props a
order = textProp "order"

orient :: String -> Props a
orient = textProp "orient"

orientation :: String -> Props a
orientation = textProp "orientation"

origin :: String -> Props a
origin = textProp "origin"

overflow :: String -> Props a
overflow = textProp "overflow"

overlinePosition :: String -> Props a
overlinePosition = textProp "overline-position"

overlineThickness :: String -> Props a
overlineThickness = textProp "overline-thickness"

panose1 :: String -> Props a
panose1 = textProp "panose-1"

paintOrder :: String -> Props a
paintOrder = textProp "paint-order"

path :: String -> Props a
path = textProp "path"

pathLength :: String -> Props a
pathLength = textProp "pathLength"

patternContentUnits :: String -> Props a
patternContentUnits = textProp "patternContentUnits"

patternTransform :: String -> Props a
patternTransform = textProp "patternTransform"

patternUnits :: String -> Props a
patternUnits = textProp "patternUnits"

ping :: String -> Props a
ping = textProp "ping"

pointerEvents :: String -> Props a
pointerEvents = textProp "pointer-events"

points :: String -> Props a
points = textProp "points"

pointsAtX :: String -> Props a
pointsAtX = textProp "pointsAtX"

pointsAtY :: String -> Props a
pointsAtY = textProp "pointsAtY"

pointsAtZ :: String -> Props a
pointsAtZ = textProp "pointsAtZ"

preserveAlpha :: String -> Props a
preserveAlpha = textProp "preserveAlpha"

preserveAspectRatio :: String -> Props a
preserveAspectRatio = textProp "preserveAspectRatio"

primitiveUnits :: String -> Props a
primitiveUnits = textProp "primitiveUnits"

r :: String -> Props a
r = textProp "r"

radius :: String -> Props a
radius = textProp "radius"

referrerPolicy :: String -> Props a
referrerPolicy = textProp "referrerPolicy"

refX :: String -> Props a
refX = textProp "refX"

refY :: String -> Props a
refY = textProp "refY"

rel :: String -> Props a
rel = textProp "rel"

renderingIntent :: String -> Props a
renderingIntent = textProp "rendering-intent"

repeatCount :: String -> Props a
repeatCount = textProp "repeatCount"

repeatDur :: String -> Props a
repeatDur = textProp "repeatDur"

requiredExtensions :: String -> Props a
requiredExtensions = textProp "requiredExtensions"

requiredFeatures :: String -> Props a
requiredFeatures = textProp "requiredFeatures"

restart :: String -> Props a
restart = textProp "restart"

result :: String -> Props a
result = textProp "result"

rotate :: String -> Props a
rotate = textProp "rotate"

rx :: String -> Props a
rx = textProp "rx"

ry :: String -> Props a
ry = textProp "ry"

scale :: String -> Props a
scale = textProp "scale"

seed :: String -> Props a
seed = textProp "seed"

shapeRendering :: String -> Props a
shapeRendering = textProp "shape-rendering"

slope :: String -> Props a
slope = textProp "slope"

spacing :: String -> Props a
spacing = textProp "spacing"

specularConstant :: String -> Props a
specularConstant = textProp "specularConstant"

specularExponent :: String -> Props a
specularExponent = textProp "specularExponent"

speed :: String -> Props a
speed = textProp "speed"

spreadMethod :: String -> Props a
spreadMethod = textProp "spreadMethod"

startOffset :: String -> Props a
startOffset = textProp "startOffset"

stdDeviation :: String -> Props a
stdDeviation = textProp "stdDeviation"

stemh :: String -> Props a
stemh = textProp "stemh"

stemv :: String -> Props a
stemv = textProp "stemv"

stitchTiles :: String -> Props a
stitchTiles = textProp "stitchTiles"

stopColor :: String -> Props a
stopColor = textProp "stop-color"

stopOpacity :: String -> Props a
stopOpacity = textProp "stop-opacity"

strikethroughPosition :: String -> Props a
strikethroughPosition = textProp "strikethrough-position"

strikethroughThickness :: String -> Props a
strikethroughThickness = textProp "strikethrough-thickness"

string :: String -> Props a
string = textProp "string"

stroke :: String -> Props a
stroke = textProp "stroke"

strokeDasharray :: String -> Props a
strokeDasharray = textProp "stroke-dasharray"

strokeDashoffset :: String -> Props a
strokeDashoffset = textProp "stroke-dashoffset"

strokeLinecap :: String -> Props a
strokeLinecap = textProp "stroke-linecap"

strokeLinejoin :: String -> Props a
strokeLinejoin = textProp "stroke-linejoin"

strokeMiterlimit :: String -> Props a
strokeMiterlimit = textProp "stroke-miterlimit"

strokeOpacity :: String -> Props a
strokeOpacity = textProp "stroke-opacity"

strokeWidth :: String -> Props a
strokeWidth = textProp "stroke-width"

style :: String -> Props a
style = textProp "style"

surfaceScale :: String -> Props a
surfaceScale = textProp "surfaceScale"

systemLanguage :: String -> Props a
systemLanguage = textProp "systemLanguage"

tabindex :: String -> Props a
tabindex = textProp "tabindex"

tableValues :: String -> Props a
tableValues = textProp "tableValues"

target :: String -> Props a
target = textProp "target"

targetX :: String -> Props a
targetX = textProp "targetX"

targetY :: String -> Props a
targetY = textProp "targetY"

textAnchor :: String -> Props a
textAnchor = textProp "text-anchor"

textDecoration :: String -> Props a
textDecoration = textProp "text-decoration"

textRendering :: String -> Props a
textRendering = textProp "text-rendering"

textLength :: String -> Props a
textLength = textProp "textLength"

to :: String -> Props a
to = textProp "to"

transform :: String -> Props a
transform = textProp "transform"

type_ :: String -> Props a
type_ = textProp "type"

u1 :: String -> Props a
u1 = textProp "u1"

u2 :: String -> Props a
u2 = textProp "u2"

underlinePosition :: String -> Props a
underlinePosition = textProp "underline-position"

underlineThickness :: String -> Props a
underlineThickness = textProp "underline-thickness"

unicode :: String -> Props a
unicode = textProp "unicode"

unicodeBidi :: String -> Props a
unicodeBidi = textProp "unicode-bidi"

unicodeRange :: String -> Props a
unicodeRange = textProp "unicode-range"

unitsPerEm :: String -> Props a
unitsPerEm = textProp "units-per-em"

vAlphabetic :: String -> Props a
vAlphabetic = textProp "v-alphabetic"

vHanging :: String -> Props a
vHanging = textProp "v-hanging"

vIdeographic :: String -> Props a
vIdeographic = textProp "v-ideographic"

vMathematical :: String -> Props a
vMathematical = textProp "v-mathematical"

values :: String -> Props a
values = textProp "values"

vectorEffect :: String -> Props a
vectorEffect = textProp "vector-effect"

version :: String -> Props a
version = textProp "version"

vertAdvY :: String -> Props a
vertAdvY = textProp "vert-adv-y"

vertOriginX :: String -> Props a
vertOriginX = textProp "vert-origin-x"

vertOriginY :: String -> Props a
vertOriginY = textProp "vert-origin-y"

viewBox :: String -> Props a
viewBox = textProp "viewBox"

viewTarget :: String -> Props a
viewTarget = textProp "viewTarget"

visibility :: String -> Props a
visibility = textProp "visibility"

width :: String -> Props a
width = textProp "width"

widths :: String -> Props a
widths = textProp "widths"

wordSpacing :: String -> Props a
wordSpacing = textProp "word-spacing"

writingMode :: String -> Props a
writingMode = textProp "writing-mode"

x :: String -> Props a
x = textProp "x"

xHeight :: String -> Props a
xHeight = textProp "x-height"

x1 :: String -> Props a
x1 = textProp "x1"

x2 :: String -> Props a
x2 = textProp "x2"

xChannelSelector :: String -> Props a
xChannelSelector = textProp "xChannelSelector"

xlinkActuate :: String -> Props a
xlinkActuate = textProp "xlink:actuate"

xlinkArcrole :: String -> Props a
xlinkArcrole = textProp "xlink:arcrole"

xlinkHref :: String -> Props a
xlinkHref = textProp "xlink:href"

xlinkRole :: String -> Props a
xlinkRole = textProp "xlink:role"

xlinkShow :: String -> Props a
xlinkShow = textProp "xlink:show"

xlinkTitle :: String -> Props a
xlinkTitle = textProp "xlink:title"

xlinkType :: String -> Props a
xlinkType = textProp "xlink:type"

xmlBase :: String -> Props a
xmlBase = textProp "xml:base"

xmlLang :: String -> Props a
xmlLang = textProp "xml:lang"

xmlSpace :: String -> Props a
xmlSpace = textProp "xml:space"

y :: String -> Props a
y = textProp "y"

y1 :: String -> Props a
y1 = textProp "y1"

y2 :: String -> Props a
y2 = textProp "y2"

yChannelSelector :: String -> Props a
yChannelSelector = textProp "yChannelSelector"

z :: String -> Props a
z = textProp "z"

zoomAndPan :: String -> Props a
zoomAndPan = textProp "zoomAndPan"
